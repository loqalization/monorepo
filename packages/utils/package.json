{
  "name": "utils",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsup src/main.ts --format cjs --dts --minify --sourcemap --watch",
    "build": "rimraf dist && tsup src/main.ts --format cjs --dts --minify --sourcemap"
  },
  "types": "dist/main.d.ts",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^5.0.1",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2",
    "zod": "^3.22.2"
  }
}
